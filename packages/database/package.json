{
  "name": "@storageguard/database",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "db:migrate": "npm run build && typeorm migration:run -d dist/datasource.js",
    "db:generate": "npm run build && typeorm migration:generate -d dist/datasource.js",
    "seed": "ts-node src/seed/run-seed.ts"
  },
  "dependencies": {
    "typeorm": "^0.3.0",
    "pg": "^8.11.0",
    "reflect-metadata": "^0.1.13"
  }
}